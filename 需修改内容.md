# 需修改内容

本文档基于当前 `codex/final-platform-with-vulnerable-app` 分支重新梳理。当前系统已经具备本地靶场、扫描核心、后端 API、前端控制台、AI 修复建议、HTML/Markdown 报告和内存级漏洞状态流转；后续修改重点从“可演示 demo”推进到“可持续使用的扫描平台”。

安全边界不变：本项目只用于本地授权靶场和课程实验，不用于扫描未授权第三方网站。

## P0 必须优先完善

### 1. 数据库持久化

- 现状：`scanner/web_api.py` 中任务、报告、资产和设置都主要保存在内存变量里，服务重启后会丢失。
- 修改目标：引入 SQLite，至少保存以下表：
  - `scan_tasks`：任务 ID、目标 URL、源码路径、状态、进度、创建时间、完成时间、错误信息。
  - `vulnerabilities`：漏洞 ID、任务 ID、类型、风险、分数、位置、检测方式、请求方法、payload、扫描规则、证据、建议来源、修复建议、状态、优先级。
  - `reports`：任务 ID、报告名称、HTML/Markdown 内容或文件路径、生成时间、风险摘要。
  - `settings`：默认目标地址、默认源码路径、AI provider、非敏感运行配置。
  - `assets`：资产名称、地址、类型、标签、负责人、最近扫描时间、风险等级。
- 验收标准：重启 `serve_app.py` 后，任务历史、漏洞列表、报告中心、资产列表和默认设置仍能恢复。

### 2. 任务恢复与报告恢复

- 现状：前端刷新能读取当前后端内存任务；后端重启后无法恢复历史。
- 修改目标：后端启动时从数据库加载最近任务、漏洞、报告和资产。
- 影响页面：扫描管理、漏洞管理、AI 修复、报告中心、资产管理、任务调度。
- 验收标准：重启服务后前端默认展示最近一次已完成扫描结果，并能继续打开对应 HTML/Markdown 报告。

### 3. 扫描前健康检查

- 现状：扫描任务启动后才暴露目标不可达或源码路径错误。
- 修改目标：在 `/api/scan/start` 前或任务创建初期检查：
  - 后端 API 是否正常。
  - 靶场 `/health` 是否可访问。
  - `base_url` 是否是本地或明确授权目标。
  - `project_path` 是否存在且可读。
- 验收标准：目标未启动或源码路径不存在时，前端给出明确错误，不进入误导性的扫描进度。

### 4. AI Key 与建议结果持久化

- 现状：前端可以输入 API Key，但只保存在当前后端进程；AI 建议来源已能显示 `qwen:qwen-plus` 或 `local-template`。
- 修改目标：
  - API Key 本身仍建议放 `.env`，不要明文入库。
  - 数据库保存 AI provider、建议内容、建议来源、生成时间和失败原因。
  - 保留“测试 AI 连接”能力，并记录最近一次测试状态。
- 验收标准：重启后仍能看到每条漏洞上一次生成的建议内容和来源。

## P1 高优先级完善

### 5. 任务控制接口

- 现状：可以启动任务、轮询状态、读取结果，但不能取消、重跑、删除。
- 修改目标：
  - `POST /api/scan/{task_id}/rerun`
  - `POST /api/scan/{task_id}/cancel`
  - `DELETE /api/scan/{task_id}`
  - `DELETE /api/tasks`
- 注意：当前扫描线程没有强中断机制，可先做软取消标记，让任务在规则之间检查取消状态。
- 验收标准：任务调度页和扫描管理页可以重跑、删除、清空任务；运行中任务可标记取消。

### 6. 正式资产库

- 现状：资产管理页展示的是扫描任务派生出的目标 URL 和源码目录。
- 修改目标：
  - 支持新增、编辑、删除资产。
  - 资产字段包括名称、地址、类型、源码路径、负责人、标签、备注、最近扫描时间、风险等级。
  - 支持从资产发起扫描。
- 验收标准：资产不依赖扫描任务生成，重启后仍存在，并可直接作为扫描目标。

### 7. 任务调度能力

- 现状：任务调度页读取后端任务队列，没有真正周期任务。
- 修改目标：
  - 支持一次性任务、每 N 分钟任务、每日固定时间任务。
  - 支持启用、停用、立即执行、删除调度。
  - 调度配置入库。
- 验收标准：不手动点击“开始扫描”也能按计划创建扫描任务，并在任务调度页看到执行历史。

### 8. 漏洞复核闭环

- 现状：支持内存级状态：未修复、修复中、已修复、已忽略、误报。
- 修改目标：
  - 状态入库。
  - 增加复核备注、复核人、更新时间。
  - 支持误报原因和忽略原因。
  - 支持按状态、风险、优先级、类型、规则 ID 搜索和筛选。
- 验收标准：重启后漏洞状态不丢失，报告能反映最新修复状态。

### 9. 漏洞去重与置信度

- 现状：扫描结果按规则直接追加，缺少统一去重和置信度字段。
- 修改目标：
  - 生成漏洞指纹：类型、位置、请求方法、payload、规则 ID、证据摘要。
  - 合并重复漏洞。
  - 增加 `confidence` 字段：High、Medium、Low。
  - 报告和前端展示置信度。
- 验收标准：重复扫描同一目标时不会产生大量重复漏洞，误报复核更清晰。

## P2 中优先级完善

### 10. 简单爬虫与路由发现

- 现状：DAST 规则覆盖靶场固定路由，包括 SQL 注入、XSS、越权、CSRF、路径穿越、SSRF、开放重定向、Mass Assignment、信息泄露。
- 修改目标：
  - 只在本地授权目标内爬取。
  - 提取同源链接、表单 action、method、input name。
  - 生成待扫描路由列表。
  - 固定规则优先，发现结果作为补充。
- 验收标准：扫描器能列出发现的页面和表单，并在报告中说明扫描覆盖范围。

### 11. 表单发现驱动的规则执行

- 现状：规则直接写死 `/login`、`/comments` 等路径。
- 修改目标：
  - SQL 注入规则优先识别登录类表单。
  - XSS 规则优先识别评论、消息、内容提交类表单。
  - CSRF 规则识别所有状态变更 POST 表单。
- 验收标准：靶场路径变更后，只要页面表单语义清晰，扫描器仍能找到目标。

### 12. SAST AST 级增强

- 现状：SAST 主要是正则检测硬编码密钥和弱密码存储。
- 修改目标：
  - 使用 Python `ast` 检测 SQL 字符串拼接、f-string SQL、`execute()` 危险调用。
  - 检测 Flask 路由中缺少登录/角色校验的敏感路径。
  - 检测 `render_template_string`、`Markup`、未转义输出风险。
  - 检测 debug/config 类接口暴露敏感数据。
- 验收标准：SAST 不只依赖关键词，能给出更准确的代码位置和规则 ID。

### 13. 报告管理增强

- 现状：报告中心可以打开后端生成的 HTML/Markdown。
- 修改目标：
  - 支持报告删除、重命名、重新生成。
  - 支持按风险等级、任务状态、时间筛选。
  - 支持导出 JSON/CSV 漏洞清单。
  - 报告内容支持中文执行摘要。
- 验收标准：报告中心不仅能打开报告，还能管理历史报告。

### 14. AI 修复建议增强

- 现状：可以对单个漏洞生成/刷新建议，也能显示建议来源。
- 修改目标：
  - 支持一键刷新全部漏洞建议。
  - 支持只刷新 P0/P1 漏洞。
  - 保存多版本建议。
  - 支持人工编辑最终修复建议。
- 验收标准：AI 修复页能批量生成、查看历史版本，并导出人工确认后的建议。

## P3 平台化完善

### 15. 登录与权限控制

- 现状：前端显示 Admin，但没有真实登录态；后端 API 没有鉴权。
- 修改目标：
  - 增加登录、退出、会话或 token。
  - 角色至少包含管理员、普通成员、只读查看。
  - 敏感接口鉴权，例如删除任务、修改设置、保存 API Key。
- 验收标准：未登录不能操作扫描；只读用户不能修改任务和设置。

### 16. 系统设置持久化与安全处理

- 现状：默认目标、源码路径、AI provider 等设置是内存级；API Key 临时写入进程环境变量。
- 修改目标：
  - 非敏感设置入库。
  - API Key 继续推荐 `.env`，前端临时输入只保存当前进程。
  - 设置页明确显示配置来源：`.env`、前端临时配置、默认值。
- 验收标准：重启后非敏感设置不丢失，密钥不进入 Git 和普通明文数据库字段。

### 17. 正式后端框架

- 现状：后端使用 Python 标准库 `http.server`。
- 修改目标：
  - 迁移到 Flask 或 FastAPI。
  - 增加 OpenAPI 文档。
  - 统一错误格式、请求校验和日志。
- 验收标准：接口结构清晰，可由前端和报告模块稳定调用。

### 18. 扫描安全策略

- 现状：项目文档声明只用于本地授权目标，但代码层限制仍较弱。
- 修改目标：
  - 默认只允许 `127.0.0.1`、`localhost`、私有网段或白名单目标。
  - 对 SSRF 测试等规则加入更明确的安全限制。
  - 前端扫描目标保存时提示授权边界。
- 验收标准：误填公网第三方 URL 时默认拒绝或强提示。

### 19. 前端真实空状态替换

- 现状：前端仍保留部分本地兜底演示数据和说明性文案。
- 修改目标：
  - 后端未连接时显示明确空状态和启动命令。
  - 后端已连接但无数据时显示“请先启动扫描”。
  - 移除容易被误解为真实结果的初始假漏洞。
- 验收标准：答辩时不会把兜底数据误认为真实扫描结果。

## 当前已完成的基础能力

- 本地靶场 `vulnerable_app/` 已整合。
- 双击 `start_system.bat` 可启动靶场、扫描平台并打开前端。
- 扫描管理可调用后端启动扫描、轮询状态、展示结果。
- DAST 已覆盖 SQL 注入、XSS、越权、CSRF、路径穿越、SSRF、开放重定向、Mass Assignment、信息泄露。
- SAST 已覆盖硬编码密钥和弱密码存储。
- 扫描事件流已细化到规则级别。
- 漏洞管理支持详情跳转到 AI 修复页。
- AI 修复建议支持 OpenAI、DeepSeek、千问兼容接口，并有本地模板兜底。
- 系统设置页支持临时输入 API Key 和测试 AI 连接。
- 报告支持 HTML/Markdown，包含执行摘要、风险统计、修复优先级、状态统计和漏洞明细。
- 漏洞状态支持内存级未修复、修复中、已修复、已忽略、误报。

## 建议实施顺序

1. 数据库持久化：任务、漏洞、报告、资产、设置。
2. 后端启动恢复历史任务和最近报告。
3. 扫描前健康检查和授权目标校验。
4. 任务控制：重跑、删除、清空、软取消。
5. 正式资产库和从资产发起扫描。
6. 调度任务入库和周期执行。
7. 漏洞复核备注、误报原因、置信度和去重。
8. 简单爬虫、表单发现和发现结果展示。
9. SAST AST 级规则增强。
10. 登录权限、正式后端框架和更严格安全策略。
